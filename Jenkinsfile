pipeline {
  agent {
    node {
      label 'dotnet '
    }
    
  }
  stages {
    stage('Build') {
      steps {
        sh '#'
        sleep 100
      }
    }
  }
  environment {
    dev = 'dev'
  }
}